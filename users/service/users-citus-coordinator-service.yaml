apiVersion: v1
kind: Service
metadata:
  name: users-citus-coordinator-deployment
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: users-citus-coordinator-deployment
  type: LoadBalancer
